version: "3.7"

networks:
  caliwears_network:
    driver: bridge

services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "caliwears_product_service"
    volumes:
      - .:/product
      - ./product/node_modules
    ports:
      - "3000:3000"
    networks:
      - caliwears_network
